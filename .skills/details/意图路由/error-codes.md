# 意图路由 — 错误码

意图路由模块内无独立错误码。所有异常统一回退为 `INTENT_CHAT` 处理。

| 异常场景 | 处理方式 |
|---------|---------|
| LLM 调用超时 | 日志记录 + 回退 INTENT_CHAT |
| JSON 解析失败 | 日志记录 + 回退 INTENT_CHAT |
| 响应包含多余格式 | `AiJsonHelper` 自动清理后重试解析 |
